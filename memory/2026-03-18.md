
## Telegram Bot 修復（2026-03-18 下午）

### 問題
兩個 Telegram bot 無法連線（`accounts 1/2`）

### 修復內容
1. **Bot 8615218979（default 帳號）token 遺失** → 從 bak.3 備份找回補上
2. **Bot 8766878615 的 `dmPolicy` 被改成 `allowlist`** → 改回 `pairing`
3. **頂層 `allowFrom` 白名單被刪除** → 補回 5 個允許的 user ID
4. **`autoSelectFamily: false`** → 改為 `true`，兩個帳號各自加上 `network` 設定
5. **Bot 8766878615 路由到 `googlemeet` agent**（已有 binding，正常）

### 結果
`accounts 2/2`，兩個 bot 正常連線
